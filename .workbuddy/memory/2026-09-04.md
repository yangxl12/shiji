# 2026-09-04

## 全局搜索 + 多选模式保持展开态

新增 `src/components/GlobalSearch/`（`GlobalSearch.tsx` + `.css`）：

- 右上角固定按钮（搜索 `right:112px` → 主题 `56px` → 同步 `0`，桌面端同样用
  `right: max(112px, calc(50vw - var(--app-max-width)/2 + 112px))` 贴内容列）。
- 弹层 portal 到 `document.body`，**顶部下探**式玻璃面板（输入框常驻顶部，软键盘弹出时结果区收缩）。
  `--kb-inset` 定义在 `.app` 上，portal 出去取不到，所以从 App 把 `keyboardInset` 当 prop 传进来，
  用内联 `style={{ bottom: keyboardInset }}` 下发。
- 搜索范围 `allNotes`：标题 + `markdownToPlainText(content)`，大小写不敏感；
  片段以命中处为中心截断（前 16 / 后 44 字符），仅标题命中时退回正文开头 60 字符。
- 高亮：`splitByKeyword` 按 `escapeRegExp` 切分，命中包 `<mark class="global-search-hit">`。

### 多选模式下保持展开（NoteCard）

- 删掉「进入批量模式时 `setExpanded(false)`」的 `wasBatchMode` 渲染期适配块；
  `isOpen` / `showOpen` 不再 `&& !isBatchMode`；`toggleExpand` 去掉批量模式早退。
- 头部展开按钮在批量模式下也渲染（按钮已 `stopPropagation`，不会误触选中/长按）。
- 批量模式 + 展开时复选框改为对齐标题行：`.note-card-batch.note-card-open .note-card-checkbox { top: 28px; }`
  （默认 `top:50%` + `translateY(-50%)`，卡片变高后复选框会跟着沉到中间）。

### 标签页顶栏

`.tags-header .tag-chip-nav` 加 `margin-right: 120px`，让出右上角 3 个 56px 按钮区，
避免窄屏 chip 被压住（chip 区本身 `overflow-x:auto`，空间不足时自动退化为横向滚动）。

## 其他

- 冒烟测试用 `playwright-core`（装在 `C:/Users/BYS/.workbuddy/binaries/node/workspace`，
  ESM 里必须写 `file:///` 绝对路径 + 默认导出解构，NODE_PATH 对 ESM 无效）+ 本机 Chrome
  `C:/Program Files/Google/Chrome/Application/chrome.exe`；种子数据直接往 IndexedDB 写，
  注意默认 Tab 是 `impromptu`，种子笔记的 category 要匹配才看得到。
- 本次 push 正常走 SSH（`git@github.com`），无需 127.0.0.1:7888 代理。
